# Copy this file to `.env` and fill in the values:
#   cp .env.example .env
#
# `.env` is gitignored and must never be committed.

# Direct Postgres connection string — the only credential the sync scripts need.
# e.g. postgresql://postgres:PASSWORD@db.heiyckeurcfjpnhmsjfo.supabase.co:5432/postgres
SUPABASE_DB_URL=

# --- Reserved for the future web app (P1) — not used by the sync scripts ---
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
